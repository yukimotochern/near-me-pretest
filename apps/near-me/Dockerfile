# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build near-me`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t near-me`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system near-me && \
          adduser --system -G near-me near-me

COPY dist/apps/near-me near-me
RUN chown -R near-me:near-me .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix near-me --omit=dev -f install

CMD [ "node", "near-me" ]
